import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Next.js configuration for CrediBridge */
  reactStrictMode: true,
  images: {
    remotePatterns: [],
  },
  typedRoutes: false,
};

export default nextConfig;
